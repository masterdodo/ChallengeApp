<html>
<head>
  <title>Index</title>
  <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
</head>
<body>
  <nav>

  </nav>
<div id="wrapper">
  <% @Challenges.each do |challenge| %>
  <div <%= "class=challenge id=challenge#{challenge.id}" %>>
    <div class="challenge_name"><%= challenge.name %></div>
    <%= image_tag(challenge.main_image) %>
  </div>
  <% end %>
</div>
</body>
</html>
